# UI Smoke Tests

These are lightweight smoke tests using ReactDOMServer to ensure components render without crashing. Run with ts-node or your TS runner.

Examples:
- npx ts-node tests/ui/navbar.smoke.tsx
- npx ts-node tests/ui/sidebar.smoke.tsx

